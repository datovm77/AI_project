# AI Coding Mentor - 智能编程导师周报系统

> **一个基于多智能体协作（Multi-Agent）与 RAG 联网搜索的个人编程成长辅助系统。**

##  项目简介

**AI Coding Mentor** 模拟了一个由多位专家组成的“虚拟代码评审委员会”。当你上传一周的学习资料（代码、报错截图、PDF文档、笔记）后，系统会自动清洗数据，并发调度不同的 AI 角色进行深度分析，最终生成一份包含 **代码纠错**、**架构评估** 和 **联网解决方案** 的深度周报。

##  核心功能

- **智能档案管理员 (Librarian Agent)**
  - 支持多模态输入：`.py`, `.java`, `.pdf`, `.docx`, `.png` (报错截图)。
  - 自动进行 ETL（抽取、转换、加载）：将杂乱文件清洗为结构化 JSON 上下文。
- **深度联网搜索 (RAG Pipeline)**
  - 集成 **Serper API** (搜索) + **Jina Reader** (网页抓取)。
  - **自动去噪**：使用便宜的小模型（Gemini Flash/DeepSeek）将几万字的网页文档压缩为精准的 JSON 答案。
- **并发专家评审**
  - **Reviewer (审计员)**：专注于安全漏洞、Bug 修复和代码规范。
  - **Architect (架构师)**：对比历史档案，评估算法复杂度与技术栈成长。
- **导师总结**
  - 综合所有报告，生成鼓励性与指导性并存的最终周报。

##  系统架构

代码段

```
graph TD
    User[用户上传一周文件] --> Librarian[档案管理员]
    
    subgraph "预处理阶段 (ETL)"
    Librarian -->|OCR & 清洗| Context[结构化数据 JSON]
    end
    
    subgraph "并发分析阶段 (Asyncio)"
    Context --> Reviewer[代码审计员]
    Context --> Architect[架构师]
    
    Reviewer -- 发现报错 --> SearchService[搜索服务模块]
    SearchService -- Google -> Jina -> AI总结 --> Reviewer
    end
    
    subgraph "总结阶段"
    Reviewer --> Mentor[导师 Agent]
    Architect --> Mentor
    Mentor --> Report[最终周报]
    end
```

## 文件结构

Plaintext

```
.
├── agent1.0.py           # [主程序] Streamlit 界面与 Agent 编排逻辑
├── search_service.py     # [独立模块] 负责联网搜索、爬取与 AI 格式化总结
├── requirements.txt      # 项目依赖库
└── .streamlit/
    └── secrets.toml      # [配置] 存放 API Key (不要上传到 GitHub)
```

## 🛠️ 快速开始

### 1. 环境准备

确保你的 Python 版本 >= 3.9。

Bash

```
# 克隆项目
git clone https://github.com/datovm77/AI_project.git
cd AI_project

# 安装依赖
pip install -r requirements.txt
```

*(依赖包含：`streamlit`, `openai`, `aiohttp`, `requests`, `pdfplumber`, `python-docx`, `Pillow`, `python-dotenv`等)*

### 2. 配置 API 密钥

在项目根目录下创建 `.streamlit/secrets.toml` 文件（如果没有文件夹请手动创建），填入以下内容：

```
# OpenRouter 或 OpenAI 的 Key（用于主模型）
API_KEY = "sk-or-v1-xxxxxxxxxxxxxxxx"

# Serper.dev 的 Key（用于联网搜索/非必要）
API_SEARCH = "xxxxxxxxxxxxxxxx"
```

### 3. 运行系统

Bash

```
streamlit run agent1.0.py
```

浏览器会自动打开 `http://localhost:8501`。

## 模块详解

### `search_service.py`

这是一个完全独立的搜索微服务。

- **输入**：查询关键词 (Query)。
- **流程**：
  1. 调用 Serper API 获取 Top 3 链接。
  2. 使用 Jina Reader 并发抓取全文。
  3. 调用廉价 AI 模型 (Gemini-Flash) 进行严格的 JSON 格式化总结。
- **输出**：标准的 List[Dict] 数据，供主程序直接调用。

### `agent1.0.py`

主入口，利用 `asyncio.gather` 实现了 Reviewer 和 Architect 的并行工作，极大地减少了用户等待时间。

### 调用模型
```c
MODEL_CONFIG = {
    "librarian": "google/gemini-3-flash-preview", 
    "reviewer": "google/gemini-3-flash-preview",
    "architect": "google/gemini-3-flash-preview",
    "mentor": "anthropic/claude-opus-4.5"          
}
```

## 待办事项 (To-Do)

- [ ] 添加历史周报的数据库存储 (SQLite/Supabase)。
- [ ] 支持更多格式的文件解析 (如 `.csv`, `.xlsx`)。
- [ ] 增加 “多轮对话” 功能。
- [ ] 增加 “模型联网” 功能。

## 贡献

欢迎提交 Issue 或 Pull Request！

------

**Made with  by [Liao Kuan]**